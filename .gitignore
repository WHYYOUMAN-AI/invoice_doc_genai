# === Virtual environments ===
venv/
env/
ENV/
.venv/

# === Python cache and bytecode ===
__pycache__/
*.py[cod]
*$py.class

# === Build / dist / packaging ===
build/
dist/
*.egg-info/
.eggs/

# === Logs / databases ===
*.log
*.sqlite3

# === Jupyter / notebooks ===
.ipynb_checkpoints/

# === Test / coverage ===
.coverage
htmlcov/
.cache/
.pytest_cache/

# === IDEs ===
.vscode/
.idea/

# === OS / system files ===
.DS_Store
Thumbs.db

# === Env / secrets ===
.env
.env.*

# === Ignore large files over 100MB ===
# This will ignore all files over 100MB (GitHub's limit)
# NOTE: Git can't filter by size in .gitignore, but you can prevent adding them via hooks or Git LFS
*.bak
*.zip
*.tar.gz
*.rar
*.7z
*.mp4
*.mov
*.avi
*.mkv
*.iso
*.exe
*.bin
*.pdf
*.pptx
*.docx
*.csv
*.tsv

# Optional: ignore files bigger than 100MB using Git hooks (see below)
